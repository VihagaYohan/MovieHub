import React, { Component } from 'react'
import {Dimensions} from 'react-native'

// constants
import {DIMENSIONS} from '.'

const PADDING = 10
const DEFAULT_WIDTH = DIMENSIONS.screenWidth - (PADDING*2)
const RADIUS = 10

export default {
    PADDING:PADDING,
    DEFAULT_WIDTH:DEFAULT_WIDTH,
    RADIUS:RADIUS
}