import React, { Component } from 'react'
import {StyleSheet,View,Text,Pressable} from 'react-native'

// componets

// constants

interface propTypes {

}

const UICard = (props:propTypes) => {
    return(
        <View></View>
    )
}

const styles = StyleSheet.create({

})

export default UICard